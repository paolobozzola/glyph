<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Quick Look app extensions MUST be sandboxed or the host won't activate them. -->
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <!-- Read the file being previewed (the QL host grants access to it). -->
    <key>com.apple.security.files.user-selected.read-only</key>
    <true/>
    <!-- WKWebView inside a sandboxed extension needs this to launch its web-content /
         networking helper processes; without it load() hangs (spinning cog). All content
         is local (app:// scheme), so no actual network egress happens. -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Shared App Group: read the user's typography/settings written by the main app, so a
         Quick Look preview renders identically to the editor. -->
    <key>com.apple.security.application-groups</key>
    <array>
        <string>group.com.paolobozzola.glyph</string>
    </array>
</dict>
</plist>
