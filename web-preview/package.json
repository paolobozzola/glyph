{
  "name": "glyph-preview",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Glyph Quick Look preview renderer (Milkdown Crepe, read-only) — built into the preview extension",
  "scripts": {
    "build": "vite build"
  },
  "dependencies": {
    "@milkdown/crepe": "^7.5.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
